# Solución — GMU Numerical Analysis Preliminary Exam, Enero 2018

**Formato:** resolver 5 de 7 problemas, libro/notas cerrados, sin calculadora.

**Sección principal del temario:** examen mixto — cubre las tres secciones del
syllabus: (1) aritmética de punto flotante y condicionamiento, (2) ecuaciones no
lineales (Newton-Raphson y su orden de convergencia), y (3) álgebra lineal
numérica (condicionamiento de sistemas lineales, complejidad de algoritmos),
más los temas adicionales de interpolación polinómica, cuadratura de Gauss y
EDO (cota de error de Euler).

---

## Problema 1 — Newton-Raphson para el recíproco de $N$

**(a)** Sea $f(x)=\dfrac1x-N$, de modo que $f(x)=0 \iff x=1/N$. Como
$f'(x)=-1/x^2$, la iteración de Newton es
$$x_{n+1}=x_n-\frac{f(x_n)}{f'(x_n)}=x_n-\frac{\frac1{x_n}-N}{-\frac1{x_n^2}}
=x_n+x_n^2\left(\frac1{x_n}-N\right)=x_n+x_n-Nx_n^2=2x_n-Nx_n^2=x_n(2-Nx_n).$$

**(b)** Sea $\alpha=1/N$ la raíz y $e_n=x_n-\alpha$. Sustituyendo directamente:
$$x_{n+1}-\alpha=2x_n-Nx_n^2-\frac1N=-N\left(x_n^2-\frac{2x_n}{N}+\frac1{N^2}\right)
=-N\left(x_n-\frac1N\right)^2=-N\,e_n^2.$$
Es decir, **exactamente** (no solo asintóticamente)
$$e_{n+1}=-N\,e_n^2 \quad\Longrightarrow\quad |e_{n+1}|=N\,|e_n|^2.$$
Si $x_n\to\alpha$ (es decir $e_n\to0$) y $N\neq0$, esto muestra que
$\dfrac{|e_{n+1}|}{|e_n|^2}\to N$, una constante finita y no nula: por
definición, la convergencia es **cuadrática** (orden 2), con constante
asintótica de error $C=N$.

*Verificación alternativa (vía Taylor de la función de iteración):* con
$g(x)=x(2-Nx)=2x-Nx^2$, $g'(x)=2-2Nx$, $g'(\alpha)=2-2N/N=0$, y $g''(x)=-2N\neq0$.
Como $g'(\alpha)=0$, el punto fijo es al menos de orden 2, y
$e_{n+1}\approx \tfrac12 g''(\alpha)e_n^2=-Ne_n^2$, coincidiendo exactamente con
el cálculo directo. $\blacksquare$

---

## Problema 2 — Interpolación polinómica

**(a) Existencia y unicidad.** Buscamos $P(t)=\sum_{k=0}^{n-1}c_k t^k$ tal que
$P(t_i)=y_i$, $i=1,\dots,n$. Esto es un sistema lineal $n\times n$ para los
coeficientes $c_k$, cuya matriz es la matriz de Vandermonde
$$V=\big(t_i^{\,k}\big)_{i=1,\dots,n;\ k=0,\dots,n-1},\qquad
\det V=\prod_{1\le i<j\le n}(t_j-t_i)\neq0,$$
porque los $t_i$ son distintos. Como $\det V\neq0$, el sistema $Vc=y$ tiene
solución única: existe un único polinomio de grado $\le n-1$ que interpola los
datos.

*Unicidad, argumento directo:* si $P,Q$ son dos polinomios de grado $\le n-1$
con $P(t_i)=Q(t_i)=y_i$ para $i=1,\dots,n$, entonces $D=P-Q$ tiene grado $\le
n-1$ y se anula en los $n$ puntos distintos $t_1,\dots,t_n$. Un polinomio no
nulo de grado $\le n-1$ tiene a lo más $n-1$ raíces, así que $D\equiv0$, es
decir $P\equiv Q$.

**Método constructivo (forma de Lagrange).** Se define
$$P(t)=\sum_{i=1}^n y_i\,L_i(t),\qquad
L_i(t)=\prod_{\substack{j=1\\ j\neq i}}^n \frac{t-t_j}{t_i-t_j}.$$
Cada $L_i$ tiene grado $n-1$, satisface $L_i(t_k)=\delta_{ik}$ (es $1$ en
$t_i$ y $0$ en los demás nodos), así que $P(t_k)=\sum_i y_i\delta_{ik}=y_k$
para todo $k$. (Alternativamente, se puede construir vía diferencias
divididas de Newton, que da el mismo polinomio en forma computacionalmente
más eficiente para agregar nodos.)

**(b) Fórmula del error para interpolación lineal.** Sea $x\in(t_1,t_2)$ fijo.
Definimos la función auxiliar (de la variable $t$)
$$\varphi(t)=f(t)-P_1(f;t)-\big[f(x)-P_1(f;x)\big]\,
\frac{(t-t_1)(t-t_2)}{(x-t_1)(x-t_2)}.$$
Como $f\in C^2[t_1,t_2]$ y $P_1$ es polinomio de grado 1, $\varphi\in
C^2[t_1,t_2]$. Además:
- $\varphi(t_1)=f(t_1)-P_1(f;t_1)-0=0$ (porque $P_1$ interpola en $t_1$),
- $\varphi(t_2)=0$ análogamente,
- $\varphi(x)=[f(x)-P_1(f;x)]-[f(x)-P_1(f;x)]\cdot1=0$.

Así $\varphi$ se anula en los **tres** puntos $t_1<x<t_2$. Por el teorema de
Rolle aplicado en $[t_1,x]$ y en $[x,t_2]$, existen $\eta_1\in(t_1,x)$,
$\eta_2\in(x,t_2)$ con $\varphi'(\eta_1)=\varphi'(\eta_2)=0$. Aplicando Rolle
de nuevo a $\varphi'$ en $[\eta_1,\eta_2]$, existe $\xi=\xi(x)\in(\eta_1,\eta_2)
\subset(t_1,t_2)$ con $\varphi''(\xi)=0$.

Derivando dos veces en $t$ (con $x$ fijo como parámetro), y usando que
$P_1''(t)\equiv0$ por ser de grado $\le1$:
$$\varphi''(t)=f''(t)-\big[f(x)-P_1(f;x)\big]\cdot\frac{2}{(x-t_1)(x-t_2)}.$$
Evaluando en $t=\xi$ y usando $\varphi''(\xi)=0$:
$$f''(\xi)=\big[f(x)-P_1(f;x)\big]\cdot\frac{2}{(x-t_1)(x-t_2)}
\ \Longrightarrow\
f(x)-P_1(f;x)=\frac{(x-t_1)(x-t_2)}{2}f''(\xi).$$
Renombrando la variable $x$ como $t$ obtenemos exactamente
$$f(t)-P_1(f;t)=(t-t_1)(t-t_2)\,\frac{f''(\xi(t))}{2},\qquad t_1<t<t_2.\ \blacksquare$$

---

## Problema 3 — Condición del problema $Ax=b$

**(a)** Sea $x=A^{-1}b$ la solución exacta y $\hat x\neq0$ una aproximación.
Definimos $\hat b=A\hat x$ (es decir, $\hat x$ es la solución **exacta** de un
problema perturbado con lado derecho $\hat b$: interpretación de error hacia
atrás). Se definen:
$$\text{RFE}=\frac{\|\hat x-x\|}{\|x\|},\qquad
\text{RBE}=\frac{\|\hat b-b\|}{\|b\|}.$$

**(b)** Como $x=A^{-1}b$ y $\hat x=A^{-1}\hat b$,
$$\hat x-x=A^{-1}(\hat b-b).$$
Por lo tanto
$$\frac{\text{RFE}}{\text{RBE}}=\frac{\|A^{-1}(\hat b-b)\|/\|x\|}{\|\hat b-b\|/\|b\|}
=\frac{\|A^{-1}(\hat b-b)\|}{\|\hat b-b\|}\cdot\frac{\|b\|}{\|x\|}.$$
Tomando el supremo sobre todo $\hat b\neq b$ (equivalentemente, sobre todo
vector $d=\hat b-b\neq0$), y usando la definición de norma de operador dada,
$$\sup_{\hat b\neq b}\frac{\|A^{-1}(\hat b-b)\|}{\|\hat b-b\|}
=\sup_{d\neq0}\frac{\|A^{-1}d\|}{\|d\|}=\|A^{-1}\|.$$
Como $\|b\|/\|x\|$ no depende de $\hat b$, se obtiene
$$\boxed{\ \kappa(\text{problema};b)=\sup_{\hat b\neq b}\frac{\text{RFE}}{\text{RBE}}
=\|A^{-1}\|\,\frac{\|b\|}{\|x\|}=\|A^{-1}\|\,\frac{\|Ax\|}{\|x\|}.\ }$$

**(c)** El número de condición de la **matriz** es $\kappa(A)=\|A\|\,\|A^{-1}\|$.
Como $\|Ax\|/\|x\|\le\sup_{v\neq0}\|Av\|/\|v\|=\|A\|$ para cualquier $x$, se
sigue
$$\kappa(\text{problema};b)=\|A^{-1}\|\frac{\|Ax\|}{\|x\|}\ \le\ \|A^{-1}\|\,\|A\|=\kappa(A).$$
La igualdad se alcanza cuando $x$ (equivalentemente $b=Ax$) está alineado con
la dirección que maximiza $\|Av\|/\|v\|$ (el vector singular derecho principal
de $A$). **Diferencia:** $\kappa(A)$ es una cota *uniforme*, la peor
amplificación posible del error relativo sobre **todos** los posibles lados
derechos $b$; el número de condición del problema para un $b$ (o $x$)
*específico* puede ser mucho menor que $\kappa(A)$ si ese $x$ no está alineado
con la dirección de máxima amplificación de $A$.

---

## Problema 4 — Cuadratura gaussiana de 2 puntos para peso $x^n$, $n$ impar

**(a) Derivación.** Buscamos $x_1,x_2,w_1,w_2$ tales que
$$\int_{-1}^1 f(x)x^n\,dx\approx w_1f(x_1)+w_2f(x_2)$$
sea exacta para $f=1,x,x^2,x^3$ (2 nodos $\Rightarrow$ 4 grados de libertad
$\Rightarrow$ exactitud garantizada hasta grado $2\cdot2-1=3$, como en la
teoría general de cuadratura gaussiana). Los momentos son
$\mu_k=\int_{-1}^1 x^{n+k}\,dx$; como $n$ es impar, $n+k$ es impar si $k$ es
par, y par si $k$ es impar. Así:
$$\mu_0=0,\qquad \mu_1=\frac{2}{n+2},\qquad \mu_2=0,\qquad \mu_3=\frac{2}{n+4}.$$
Las cuatro ecuaciones de exactitud son:
$$w_1+w_2=0,\qquad w_1x_1+w_2x_2=\frac2{n+2},\qquad
w_1x_1^2+w_2x_2^2=0,\qquad w_1x_1^3+w_2x_2^3=\frac2{n+4}.$$
De la primera, $w_2=-w_1$. Sustituyendo en la tercera:
$w_1(x_1^2-x_2^2)=w_1(x_1-x_2)(x_1+x_2)=0$; como $w_1\neq0$ y $x_1\neq x_2$,
se obtiene $x_2=-x_1$ (nodos simétricos). Entonces la segunda ecuación da
$$w_1(x_1-x_2)=2w_1x_1=\frac2{n+2}\ \Longrightarrow\ w_1x_1=\frac1{n+2},$$
y la cuarta, con $x_2^3=-x_1^3$,
$$w_1(x_1^3-x_2^3)=2w_1x_1^3=\frac2{n+4}\ \Longrightarrow\ w_1x_1^3=\frac1{n+4}.$$
Dividiendo: $x_1^2=\dfrac{n+2}{n+4}$, es decir
$$\boxed{x_1=\sqrt{\frac{n+2}{n+4}},\quad x_2=-x_1,\quad
w_1=\frac1{(n+2)x_1}=\frac{\sqrt{n+4}}{(n+2)^{3/2}},\quad w_2=-w_1.}$$
La regla es
$$\int_{-1}^1 f(x)x^n\,dx\approx w_1\big[f(x_1)-f(-x_1)\big].$$

**(b) Aplicación a $f(x)=x^4$.** Valor exacto:
$\int_{-1}^1 x^4\cdot x^n\,dx=\int_{-1}^1 x^{n+4}dx=0$, porque $n+4$ es impar
(integral de una función impar en un intervalo simétrico). Valor de la regla:
como $f$ es par, $f(x_1)=f(-x_1)=x_1^4$, así
$$w_1[f(x_1)-f(-x_1)]=w_1\cdot0=0.$$
Ambos coinciden exactamente: **el error es $0$** para $f=x^4$, lo cual en
particular es $O(n^{-3})$ (de hecho, más fuerte: es $O(n^{-k})$ para todo $k$).

*Explicación estructural:* como el peso $x^n$ es una función **impar** de $x$
($n$ impar) y los nodos $\{x_1,-x_1\}$ son simétricos con pesos antisimétricos
$\{w_1,-w_1\}$, tanto el funcional exacto $I(f)=\int f(x)x^n dx$ como el de la
regla $Q(f)=w_1[f(x_1)-f(-x_1)]$ dependen **solo** de la parte impar de $f$,
$f_{\text{odd}}(x)=\tfrac12[f(x)-f(-x)]$; toda potencia par de $x$ (incluido
$x^4$) contribuye automáticamente $0$ a ambos lados. La regla, diseñada para
ser exacta en el subespacio de polinomios impares $\{x,x^3\}$ (2 condiciones,
2 incógnitas libres $x_1,w_1$), no puede en cambio reproducir $x^5$: para
$f(x)=x^5$ el valor exacto es $\int x^{n+5}dx=\dfrac{2}{n+6}$ y el de la regla
es $2w_1x_1^5=\dfrac{2(n+2)}{(n+4)^2}$ (usando $w_1x_1=1/(n+2)$ y
$x_1^4=(n+2)^2/(n+4)^2$); su diferencia es
$$E(x^5)=\frac{2}{n+6}-\frac{2(n+2)}{(n+4)^2}
=\frac{2\big[(n+4)^2-(n+2)(n+6)\big]}{(n+6)(n+4)^2}
=\frac{8}{(n+6)(n+4)^2}=O(n^{-3})\quad(n\to\infty),$$
usando $(n+4)^2-(n+2)(n+6)=(n^2+8n+16)-(n^2+8n+12)=4$. Esto confirma,
mediante el primer monomio impar no reproducido por la regla, exactamente el
comportamiento asintótico $O(n^{-3})$ enunciado. $\blacksquare$

---

## Problema 5 — Condición de Hölder e iteración de punto fijo

**(a)** Sea $e_0=|x-y|$ y, para $\alpha\neq1$, $c=L^{1/(1-\alpha)}$ (bien
definido y positivo porque $L>0$). Probamos por inducción sobre $n\ge1$ que
$$|f^n(x)-f^n(y)|<c\left(\frac{e_0}{c}\right)^{\alpha^n}=:E_n.$$

*Base $n=1$:* la hipótesis del enunciado da directamente
$|f(x)-f(y)|<L|x-y|^\alpha=Le_0^\alpha$. Como $c^{1-\alpha}=L$,
$$E_1=c\left(\frac{e_0}{c}\right)^{\alpha}=c^{1-\alpha}e_0^\alpha=Le_0^\alpha,$$
así que la desigualdad base es exactamente la hipótesis dada.

*Paso inductivo:* supongamos $|f^n(x)-f^n(y)|<E_n$. Aplicando la condición de
Hölder a los puntos $f^n(x)$, $f^n(y)$, y usando que $t\mapsto t^\alpha$ es
estrictamente creciente para $\alpha>0$:
$$|f^{n+1}(x)-f^{n+1}(y)|=|f(f^n(x))-f(f^n(y))|<L\,|f^n(x)-f^n(y)|^\alpha<L\,E_n^\alpha.$$
Calculamos
$$L\,E_n^\alpha=L\left[c\left(\frac{e_0}{c}\right)^{\alpha^n}\right]^\alpha
=L\,c^\alpha\left(\frac{e_0}{c}\right)^{\alpha^{n+1}}.$$
Como $Lc^\alpha=c^{1-\alpha}c^\alpha=c$, esto es exactamente
$c\left(\dfrac{e_0}{c}\right)^{\alpha^{n+1}}=E_{n+1}$. Luego
$|f^{n+1}(x)-f^{n+1}(y)|<E_{n+1}$, completando la inducción. $\blacksquare$

**(b)** Sea $\hat x$ punto fijo, $f(\hat x)=\hat x$, y $e_n:=|f^n(x)-\hat x|
=|f^n(x)-f^n(\hat x)|$ (ya que $f^n(\hat x)=\hat x$). Por (a),
$e_n<c(e_0/c)^{\alpha^n}$ con $e_0=|x-\hat x|$.

- **Caso $\alpha>1$.** Aquí $\alpha^n\to\infty$. Si $e_0<c$ (es decir
  $e_0/c<1$), entonces $(e_0/c)^{\alpha^n}\to0$ (base $<1$ elevada a un
  exponente $\to\infty$), así $e_n\to0$: **la iteración converge a $\hat x$**
  siempre que el punto inicial satisfaga
  $$|x-\hat x|<c=L^{1/(1-\alpha)}.$$
  Más aún, la relación exacta de un paso $e_{n+1}<Le_n^\alpha$ (tomada de la
  hipótesis) muestra que la convergencia es de **orden $\alpha$**
  (superlineal), análoga en velocidad a Newton cuando $\alpha=2$. Si
  $e_0\ge c$, el argumento no da ninguna garantía.
- **Caso $\alpha=1$.** Entonces $c$ no está definido ($1-\alpha=0$); la
  hipótesis se reduce a la condición de Lipschitz usual, $|f(x)-f(y)|<L|x-y|$.
  Éste es el caso clásico del teorema de la aplicación contractiva: **la
  iteración converge a $\hat x$ para *todo* punto inicial $x\in\mathbb R$ si y
  solo si $L<1$** (contracción global); si $L\ge1$ no hay garantía.
- **Caso $0<\alpha<1$.** Ahora $\alpha^n\to0$, así que para **cualquier**
  $e_0>0$ fijo, $(e_0/c)^{\alpha^n}\to (e_0/c)^0=1$, y la cota se convierte en
  $e_n\lesssim c$: la cota **no** decae a $0$, sino que tiende a la constante
  positiva $c$. Esto refleja que $\hat x$ es, en este régimen, un punto fijo
  típicamente **repulsor** de la dinámica mayorante (en la recursión
  $t\mapsto Lt^\alpha$, $t=0$ es repulsor y $t=c$ es atractor, ya que cerca de
  $0$, $Lt^\alpha\gg t$ para $\alpha<1$). Por lo tanto, la condición de Hölder
  con $\alpha<1$ **no garantiza convergencia** de la iteración de punto fijo a
  $\hat x$ para ningún punto inicial $x\neq\hat x$ — de hecho es consistente
  con divergencia local (ejemplo: $f(x)=\hat x+2|x-\hat x|^{1/2}\,\mathrm
  sgn(x-\hat x)$ cerca de $\hat x$ aleja los puntos vecinos).

---

## Problema 6 — Conteo de operaciones en sustitución hacia atrás

Para cada valor fijo de $i$ (recorriendo $i=n,n-1,\dots,1$), el bucle interno
en $j$ recorre $j=i+1,\dots,n$, es decir $n-i$ valores, y cada uno ejecuta
**una multiplicación** ($u_{ij}\cdot x_j$). Luego, fuera del bucle interno, se
realiza **una división** ($y_i/u_{ii}$).

**Multiplicaciones:**
$$\sum_{i=1}^n (n-i)=\sum_{k=0}^{n-1}k=\frac{n(n-1)}{2}.$$

**Divisiones:** una por cada $i=1,\dots,n$, es decir $n$ divisiones.

**Total combinado (multiplicaciones + divisiones):**
$$\frac{n(n-1)}{2}+n=\frac{n(n-1)+2n}{2}=\frac{n(n+1)}{2}.$$

*Verificación:* para $n=1$ (sistema $1\times1$), la fórmula da $1$ división y
$0$ multiplicaciones, total $1=\frac{1\cdot2}{2}$. ✓. Para $n=2$: $1$
multiplicación ($i=1,j=2$) y $2$ divisiones, total $3=\frac{2\cdot3}{2}$. ✓

---

## Problema 7 — Cota de error global del método de Euler

Sea $e_i=|y_i-y(t_i)|$. Restando la expansión de Taylor exacta y la fórmula
de Euler:
$$y(t_{i+1})=y(t_i)+hf(t_i,y(t_i))+\frac{h^2}{2}y''(\xi_i),\qquad
y_{i+1}=y_i+hf(t_i,y_i),$$
$$y_{i+1}-y(t_{i+1})=\big[y_i-y(t_i)\big]+h\big[f(t_i,y_i)-f(t_i,y(t_i))\big]
-\frac{h^2}{2}y''(\xi_i).$$
Tomando valor absoluto, la desigualdad triangular, la condición de Lipschitz
$|f(t_i,y_i)-f(t_i,y(t_i))|\le L|y_i-y(t_i)|$, y $|y''(\xi_i)|\le M$:
$$e_{i+1}\le (1+hL)\,e_i+\frac{h^2M}{2},\qquad e_0=0\ \ (\text{condición
inicial exacta}).$$

**Resolviendo la recursión.** Ésta es una recursión lineal de primer orden.
Iterando (o por inducción, verificando que la fórmula satisface la
recursión con igualdad como cota superior):
$$e_i\le\frac{h^2M}{2}\sum_{k=0}^{i-1}(1+hL)^k
=\frac{h^2M}{2}\cdot\frac{(1+hL)^i-1}{(1+hL)-1}
=\frac{hM}{2L}\Big[(1+hL)^i-1\Big].$$
(*Verificación por inducción de esta suma geométrica:* para $i=0$ ambos lados
son $0$; si $e_i\le\frac{hM}{2L}[(1+hL)^i-1]$, entonces
$e_{i+1}\le(1+hL)e_i+\frac{h^2M}2\le \frac{hM}{2L}(1+hL)\big[(1+hL)^i-1\big]+\frac{h^2M}2
=\frac{hM}{2L}\big[(1+hL)^{i+1}-(1+hL)\big]+\frac{hM}{2L}(hL)
=\frac{hM}{2L}\big[(1+hL)^{i+1}-1\big]$, cerrando la inducción.)

**Cota final.** Usando la desigualdad elemental $1+z\le e^z$ (válida para todo
$z\in\mathbb R$, en particular $z=hL>0$), se tiene
$(1+hL)^i\le e^{ihL}=e^{L(t_i-a)}\le e^{L(b-a)}$ para $t_i\le b$. Por lo tanto
$$\boxed{\ |y_i-y(t_i)|=e_i\le\frac{hM}{2L}\Big[e^{L(b-a)}-1\Big],\qquad
i=0,1,\dots,n.\ }$$

**Verificación de casos límite:** si $M=0$ (es decir $y''\equiv0$, la solución
exacta es afín en $t$, reproducida exactamente por Euler en ese caso
especial... más precisamente si $y$ es lineal el error de truncamiento local
es cero) la cota da $e_i=0$ para todo $i$, consistente. Si $h\to0$ con $n=
(b-a)/h\to\infty$, la cota $\to0$ linealmente en $h$, confirmando que Euler es
un método de **orden 1** (convergencia global $O(h)$). $\blacksquare$
